package configuration

import (
	. "github.com/onsi/ginkgo/v2"
	. "github.com/onsi/gomega"
)

var _ = Describe("Configutation parsing", func() {
	It("should parse a valid configuration file", func() {
		Expect(true).To(BeTrue())
	})
})
